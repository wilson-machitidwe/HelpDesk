## Frontend (Netlify build-time)
# Keep blank to use same-origin /api routes via Netlify redirects.
VITE_API_BASE=

## Backend (Netlify serverless runtime)
JWT_SECRET=replace-with-a-long-random-secret

# Set one of these database URLs.
SUPABASE_DB_URL=postgresql://user:password@host:5432/postgres
# DATABASE_URL=
# POSTGRES_URL=

## Supabase storage (required only if you use file uploads)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key
SUPABASE_STORAGE_BUCKET=uploads

## Optional SMTP
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

## Postgres SSL
PGSSLMODE=require
